spring:
  application:
    name: product-service
server:
  port: 8081

management:
  endpoints:
    web:
      exposure:
        include: "*" # Shows all Actuator endpoints
  endpoint:
    health:
      show-details: always # Shows details of health status
